name: Publish to NuGet

on:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Install .NET 10+ SDK. Remove this step once runners ship with it.
      # If .NET 10 is still in preview, add: include-prerelease: true
      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Validate tag matches .csproj version
        run: ./tools/check-tag-version.sh "${GITHUB_REF_NAME}"

      - name: Restore
        run: dotnet restore

      - name: Build
        run: |
          dotnet build src/Houtamelo.Spire/Houtamelo.Spire.csproj -c Release --no-restore
          dotnet build tests/Houtamelo.Spire.Analyzers.Tests/Houtamelo.Spire.Analyzers.Tests.csproj -c Release --no-restore
          dotnet build tests/Houtamelo.Spire.SourceGenerators.Tests/Houtamelo.Spire.SourceGenerators.Tests.csproj -c Release --no-restore
          dotnet build tests/Houtamelo.Spire.PatternAnalysis.Tests/Houtamelo.Spire.PatternAnalysis.Tests.csproj -c Release --no-restore

      - name: Test
        run: |
          dotnet test tests/Houtamelo.Spire.Analyzers.Tests/Houtamelo.Spire.Analyzers.Tests.csproj -c Release --no-build
          dotnet test tests/Houtamelo.Spire.SourceGenerators.Tests/Houtamelo.Spire.SourceGenerators.Tests.csproj -c Release --no-build
          dotnet test tests/Houtamelo.Spire.PatternAnalysis.Tests/Houtamelo.Spire.PatternAnalysis.Tests.csproj -c Release --no-build

      - name: Pack all projects
        run: |
          dotnet pack src/Houtamelo.Spire.Core/ -c Release --no-build -o ./nupkgs
          dotnet pack src/Houtamelo.Spire.Analyzers/ -c Release --no-build -o ./nupkgs
          dotnet pack src/Houtamelo.Spire.CodeFixes/ -c Release --no-build -o ./nupkgs
          dotnet pack src/Houtamelo.Spire/ -c Release --no-build -o ./nupkgs

      - name: Push to NuGet
        run: >
          dotnet nuget push
          ./nupkgs/*.nupkg
          --source https://api.nuget.org/v3/index.json
          --api-key ${{ secrets.NUGET_API_KEY }}
          --skip-duplicate
