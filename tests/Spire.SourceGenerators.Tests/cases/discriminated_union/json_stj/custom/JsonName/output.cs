// <auto-generated/>
#nullable enable
using System;
using System.Text.Json;
using System.Text.Json.Serialization;

internal sealed class ShapeStjConverter : JsonConverter<Shape>
{
    public override Shape Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        using var doc = JsonDocument.ParseValue(ref reader);
        var root = doc.RootElement;
        if (!root.TryGetProperty("kind", out var kindProp))
            throw new JsonException("Missing discriminator property 'kind'");
        var kindStr = kindProp.GetString();
        return kindStr switch
        {
            "circle" => Shape.Circle(root.GetProperty("r").Deserialize<double>(options)!),
            "rect" => Shape.Rectangle(root.GetProperty("width").Deserialize<float>(options)!, root.GetProperty("height").Deserialize<float>(options)!),
            "pt" => Shape.Point(),
            _ => throw new JsonException($"Unknown Shape variant: {kindStr}")
        };
    }

    public override void Write(Utf8JsonWriter writer, Shape value, JsonSerializerOptions options)
    {
        writer.WriteStartObject();
        switch (value.tag)
        {
            case Shape.Kind.Circle:
                writer.WriteString("kind", "circle");
                writer.WritePropertyName("r");
                JsonSerializer.Serialize(writer, value.circle_radius, options);
                break;
            case Shape.Kind.Rectangle:
                writer.WriteString("kind", "rect");
                writer.WritePropertyName("width");
                JsonSerializer.Serialize(writer, value.rectangle.width, options);
                writer.WritePropertyName("height");
                JsonSerializer.Serialize(writer, value.rectangle.height, options);
                break;
            case Shape.Kind.Point:
                writer.WriteString("kind", "pt");
                break;
            default:
                throw new JsonException($"Unknown Shape variant: {value.tag}");
        }
        writer.WriteEndObject();
    }
}

[global::System.Text.Json.Serialization.JsonConverter(typeof(ShapeStjConverter))]
partial struct Shape { }
