# Houtamelo.Spire.Analyzers

This package contains the Roslyn analyzers and source generators for Spire.

**Do not depend on this package directly.** Use the meta-package instead:

```shell
dotnet add package Houtamelo.Spire
```

`Houtamelo.Spire` includes this package along with the core attributes, code fixes, and source generators.
